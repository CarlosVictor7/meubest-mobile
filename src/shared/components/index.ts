// Barrel file — importar todos os componentes compartilhados de um só lugar
// Ex: import { Button, Card, Avatar } from '@shared/components';

export { Button } from './Button';
export { Card } from './Card';
export { Avatar } from './Avatar';
